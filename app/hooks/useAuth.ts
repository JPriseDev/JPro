// Diese Datei ist jetzt Teil von AuthContext.tsx und wird dort exportiert.
// Sie kann leer bleiben oder gelöscht werden, wenn useAuth direkt aus AuthContext.tsx importiert wird.
// Zur Klarheit belasse ich sie vorerst als Verweis, dass der Hook nun in AuthContext.tsx definiert ist.

/**
 * @module useAuth
 * @description Dieser Hook ist nun in `app/contexts/AuthContext.tsx` definiert und wird von dort exportiert.
 * Bitte importieren Sie `useAuth` direkt aus `@/contexts/AuthContext`.
 *
 * Beispiel:
 * import { useAuth } from '@/contexts/AuthContext';
 * const { user, session, loading, signOut } = useAuth();
 */
export {}; // Leerer Export, um es als Modul zu kennzeichnen.